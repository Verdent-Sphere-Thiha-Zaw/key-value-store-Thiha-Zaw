name: "Main Workflow"
on:
    pull_request:
      branches:
        - dev
jobs:
  build:
    name: "Build Laravel App"
    runs-on: ubuntu-latest
    steps: 
      - name: Install dependencies
        run: 
          composer install
      - name: Install dependencies
        run: 
          php artisan key:generate